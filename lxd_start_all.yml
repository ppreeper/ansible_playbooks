---
- hosts: localhost
  connection: local
  vars:
    lxdgroup: ""
  tasks:
    - name: start containers
      loop: "{{ query('inventory_hostnames', lxdgroup) }}"
      lxd_container:
        name: "{{ item }}"
        state: started
